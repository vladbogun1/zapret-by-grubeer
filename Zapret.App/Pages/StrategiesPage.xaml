<Page x:Class="Zapret.App.Pages.StrategiesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Strategies">

    <Grid Margin="{StaticResource PageMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TextBlock Grid.Row="0"
                      FontTypography="Subtitle"
                      Text="Bypass strategies" />

        <ui:TextBlock Grid.Row="1"
                      Margin="0,4,0,12"
                      Appearance="Secondary"
                      TextWrapping="Wrap"
                      x:Name="SubtitleText"
                      Text="Discovered from the installed engine build. A new upstream strategy appears here on its own." />

        <ListView Grid.Row="2" x:Name="StrategyList" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,6,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ui:SymbolIcon Grid.Column="0"
                                       Symbol="{Binding Glyph}"
                                       Margin="0,0,12,0"
                                       VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1">
                            <ui:TextBlock Text="{Binding DisplayName}" FontTypography="BodyStrong" />
                            <ui:TextBlock Text="{Binding Detail}"
                                          Appearance="Secondary"
                                          TextWrapping="Wrap" />
                        </StackPanel>

                        <ui:Badge Grid.Column="2"
                                  Content="Selected"
                                  Appearance="Success"
                                  VerticalAlignment="Center"
                                  Visibility="{Binding SelectedVisibility}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <ui:Button x:Name="ApplyButton"
                       Content="Apply strategy"
                       Icon="{ui:SymbolIcon Checkmark24}"
                       Appearance="Primary"
                       Click="OnApply" />
            <ui:Button x:Name="TestButton"
                       Content="Test strategies"
                       Icon="{ui:SymbolIcon Beaker24}"
                       Margin="8,0,0,0"
                       Click="OnRunTests" />
            <ui:Button x:Name="RefreshButton"
                       Content="Refresh"
                       Icon="{ui:SymbolIcon ArrowSync24}"
                       Margin="8,0,0,0"
                       Click="OnRefresh" />
            <ui:ProgressRing x:Name="Busy"
                             IsIndeterminate="True"
                             Width="20" Height="20"
                             Margin="12,0,0,0"
                             Visibility="Collapsed" />
        </StackPanel>
    </Grid>
</Page>
